## Interactive session 2026-09-24 (prompt 10: audit the auditor)

- Built `audit/recompute.js`: Node, no dependencies, no code shared with `bin/score.py`; recomputes every headline number from the ledgers at the commit that holds `docs/data.json`, compares against data.json and TRACK-RECORD.md, exit 1 on any disagreement. `audit/README.md` is the prose spec, written for a stranger. `audit/selftest.sh` builds synthetic ledgers, checks agreement on hand-worked values, then tampers a penny and a ledger and checks both are caught. Passed 3/3.
- `.github/workflows/audit.yml`: 1st of the month 06:17 UTC, on demand, and on pushes to the scored files. Badge on the lab page and README. The token has the `workflow` scope, so it pushed.
- Found in the scorer by writing the spec, all fixed in `bin/score.py` and `bin/build-lab.cjs`: lab page said "0.25 is a coin flip" for a three-outcome Brier (0.667 is right), live since 22 Sep; market Brier was not guaranteed on the same matches as the label claimed (now both over the paired set, with paired count and paired difference published); `commit` stamp in data.json is the parent of the commit holding the inputs (data.json now carries input blob ids and the audit checks them); `state/spend.jsonl` was gitignored so the page's spend figure had no committed input (un-ignored); `fnum` accepted nan/inf; page copy said "under 24 hours" and "Dixon-Coles plus Elo" against the rules.
- Weekly SKILL step 2 now runs the recompute on the worktree and puts any disagreement at the very top of Field Notes. W39 draft leads with "The scorer was wrong on its own page".
- Left alone: `bin/review.py` and `PASS-MARKS.md`, being edited by another session at the same time (review.py shares the nan-accepting `fnum`; one line, for that session or the next). Committed my paths only.
- Denied: none (interactive). Learned: Python `round` and JS `toFixed` differ at exact binary halves, so the audit tests "is the published number a correct rounding of the exact value" with a half-unit tolerance, not string equality.

## Interactive session 2026-09-24 08:27, sub-agent guardrail test (Luke's prompt 4)

- Question: does a sub-agent inherit the unattended hook and the write roots. Answer: yes.
- One general-purpose child, 11 fixed calls. Five that should fail, failed: Write outside roots,
  Read /etc/hosts, `;` in Bash, touch outside roots, nested Agent. Four that should pass, passed.
- The hook receives the parent's session_id for a child's calls plus `agent_id` and `agent_type`.
  Child lines are in today's decisions log under session 7b8d0e87, agent a1839ba6306427ab1.
- Hook change: log line now carries agent_id, agent_type, cwd, keys. No decision logic touched.
  test-hook.py 48/48 before the probe. Those test lines are in today's log under `test-ses`.
- Denied this session: 6, all expected (5 from the child, 1 parent control). None routed around.
- Report and proposed charter wording: experiments/2026-09-24-subagent-guardrails/REPORT.md. Charter unchanged.
- Cost: child 56k tokens, 42 s. Guide agent for the docs: 57k tokens.
- Follow-up, same session, at Luke's ask: fan-out caps built into the hook behind
  `FANOUT_ENABLED = False` (4 spawns, depth 1, haiku/sonnet stated, children write sandbox/ and
  state/agents/ only, no git, scripts from sandbox/ only). test-hook.py 93/93. Ban still in force.
- Report moved out of sandbox/ (gitignored) to experiments/. Summary for Luke on the Desktop.

## Interactive session 2026-09-24 (prompt 6: pre-register the pass mark)
- Found: the charter names the week-8 review but no criteria. Nothing pre-registered the standard Proteus is judged by.
- Wrote `PASS-MARKS.md` before any position, prediction or shipped note existed: Grinder in positions (40 floor, 100 final, +£0.50 keep, -£0.50 kill, executor at 200 and +£1.00); Pitch process gates at week 8 (80% coverage, early kill at +0.015 paired Brier) and an edge test at 500 matches (edge -0.005 with CI below zero, ingredient by pool weight 0.10, else kill); Field Notes kill at 3 consecutive or 4 of 8 fails, whole persona.
- Said eight weeks is the wrong unit for the Grinder (positions, not weeks) and the wrong horizon for the Pitch (500 matches or 31 May 2027). Inconclusive converts to CHANGE with one fix and one second date, 13 Dec 2026; inconclusive twice is kill.
- Wrote `bin/review.py` to compute the verdicts from the ledgers; tested on the empty ledgers (all INCONCLUSIVE) and on synthetic ones (kill paths fire).
- Reran the Pitch backtest to anchor the lines: 930 matches, model Brier 0.6373 v closing 0.6201, gap +0.017, pool weight 0.00, paper -23% over 308 bets. Slightly different from 24 Sep's first run because the cache now holds a fourth season.
- Left alone: another session's uncommitted `pitch/data.py` (multi-league) and untracked `pitch/models.py`. Committed only my files by path.
- Denied: none. Learned: the fee model makes a £5 position break even at +49%, which the Grinder's pass mark now says out loud.

## Interactive session 2026-09-24 08:00 to 09:00 (Luke's prompts 2 and 3, international break)

Pulled: football-data for SP1, D1, I1, F1, N1, P1, SC0 (three seasons) and 2022-23 plus 2023-24 for
all nine leagues, 27 files. Understat: blocked (stub page, no data). football-data xG: 2026-27 only.
GeckoTerminal trending 1h/6h/24h probed; public RPC getTokenLargestAccounts: 429 on first call.

Built: `pitch/models.py` (dc, elo, sot), `pitch/backtest.py` (walk-forward, weekly refit, per-country
groups in parallel, report). Run: 20,298 model rows, 6,766 scored matches, 41 minutes wall clock
(England is the slow group). `pitch/backtest/REPORT.md` and `summary.json` committed.

Result: every model 0.00 pool weight against the closing line, every league, both full seasons.
Paper rule loses everywhere. Verdict and decisions in `pitch/RULES.md` note of 2026-09-24: stakes
parked, nine leagues live from 9 Oct, live model stays v0 DC as the out-of-sample check.

Grinder: feed reordered, top-10 from rugcheck with pool account excluded, gate unchanged, reasoning
in `grinder/RULES.md`. Scan on fixed feed: 105 rows, 8 pass. G-0001 to G-0004 opened 07:36 UTC,
committed 72af0c9 before anything was knowable.

Denied: none (interactive session, hook not in scope). Learned: rugcheck's topHolders includes the
AMM pool (7.5 percent on SI); `predict.py` printed the last kickoff on file as "next", fixed to the
earliest future one; pandas reads season codes like 2425 as integers unless told otherwise.
- 09:00, at Luke's word: FANOUT_ENABLED set to True, Fan-out section pasted into CHARTER.md with a dated
  amendment under the signature. test-hook.py 93/93 with the feature on. Sub-agents allowed inside the caps from tonight.
- 09:30, at Luke's word (prompt 7, HALT from a phone): kill switch now has three sources, all in
  `bin/halt-check.py`: the local file (wins, never cleared by code), a HALT file on origin/main, and an
  open GitHub issue titled HALT whose author login is on HALT_AUTHORS (triton-xxix only for now). Telegram
  considered and declined: new secret, new inbound surface, a send the charter forbids. Fail closed: any
  error in the check is a halt (exit 4), logged as "HALT check FAILED". Remote halts mirror into the local
  file; the hook now refuses git add/commit/push, send-field-notes.sh and Agent spawns while it exists.
  Wired into run-nightly.sh, send-field-notes.sh, both SKILLs (nightly re-checks before its commit; weekly
  pastes `--report` as a `## Kill switch` block every week). test-hook.py 108/108 (15 new halted cases);
  test-halt-check.py 19/19 against a bare repo, a file:// issues feed, a refused remote and two hung
  sockets (timeout fires in under 15 s). Live check against GitHub: CLEAR. Not tested for real: an actual
  issue from the phone. Luke needs the GitHub app signed in as triton-xxix, or his own login added to
  HALT_AUTHORS; either is one line.

## Interactive session 2026-09-24 09:15 (Luke's prompt 5: the probe loop)

- Built `bin/probe.py`: queue and verdicts in `state/probes.json`, register rendered to `PROBES.md`, one loop file per start under `state/probe-loop/`. `next` checks HALT, deadline, an 8-minute floor, the probe cap and the hook-logged call cap (the token proxy), then pops the first runnable item; `verdict` counts calls and denials since the probe began, writes the register and run-log lines, commits and pushes those paths, or logs without committing under HALT. Stops itself, with the reason, when the queue is empty or everything left needs something I do not have.
- Nightly SKILL: step 6 is the loop, sequential, no sub-agents; run log moved to step 7. Deadline comes from the preflight header (80 min after it), capped at 60 min of loop. Shakedown in a scratch repo: HALT, deadline, in-progress guard, session-scoped denial count, honest STOP reasons all behaved; one defect found and fixed (two loops a day shared a file name).
- Seeded twelve probes from the backlog and the persona, one declared blocked on a Lichess account, one dated for 22 Oct. Then ran the loop for real, interactive, 40-minute budget, cap 3:
- Probe loop started 09:20: deadline 10:00 (--minutes 40), 150 calls, 3 probes, session interactive.
- Probe P-0001, **works** (1 min): graphInsidersDetected is populated on 6 of 8 gate-passers (median 5, max 37) and 16 of 30 non-passers, but the per-holder insider flag and insider risks are empty on all 38 reports, and the count grows with token age (BOME 2483). Measurable, not yet a gate: it would have excluded NPC (15) and FUNKOS (5) at zero-only. Rule unchanged. Artefact: experiments/2026-09-24-P-0001
- Probe P-0002, **works** (0 min): Not the volume gate. On the fixed feed (24 Sep, 72 in-window rows) the next binding gate is top-10 share at 30 percent: 11 of 72 pass it, dropping it alone lifts entries 8 to 11, three rows fail on it alone. Dropping vol1h gives 9 and one row fails on it alone. Before the feed fix nothing passed liquidity so no gate was next. Artefact: experiments/2026-09-24-P-0002
- Probe P-0003, **not-worth-it** (1 min): Rescored the saved walk-forward probabilities against Pinnacle, average and max closing prices, no refit: all three lines within 0.001 Brier on E0 (n=590) and nine leagues (n=4639), and every model earns the identical pool weight against each. Pinnacle columns are absent for all 2026-27 and 45 percent of 2025-26, so it cannot be the live line. Keep the average close. Artefact: experiments/2026-09-24-P-0003
- Probe loop stopped 09:24: probe cap of 3 reached. 3 probe(s) (works 2, not-worth-it 1), 4 min, calls not measured.
- Left alone: another session's uncommitted `bin/score.py`. Committed my paths only. Denied: none (interactive, hook not in scope; the loop could not count calls and said so).
- Learned: the cache does not keep the rugcheck report, only the DexScreener pair, so any rugcheck field costs a refetch; football-data drops Pinnacle closing columns on the current season; zsh will not word-split a variable holding a command.

## Interactive session 2026-09-24 09:33, the loop again with the rest of the budget (Luke's word)

- Started with `--until 10:00`, the remainder of the first run's 40-minute budget. It skipped P-0004 (30 min) as not fitting and ran four probes, then stopped itself with 21 minutes left because nothing runnable remained. Not overridden.
- Probe loop started 09:33: deadline 10:00 (--until 10:00), 150 calls, 6 probes, session interactive.
- Probe P-0005, **works** (2 min): GeckoTerminal new_pools lists pump.fun bonding-curve launches as dex pump-fun beside pumpswap, so both sides are keyless from one feed: 46 launches and 10 pumpswap pools in 3.6 minutes, about 22 percent, an upper bound because untraded launches are never listed. pump.fun API 403 geo-blocked, rechecked. Public RPC cannot count creations: 1000 signatures span 24 seconds and a 22-tx sample found no Create. A day needs a 5-minute poller, queued. Artefact: experiments/2026-09-24-P-0005
- Probe P-0007, **blocked** (1 min): Every listing endpoint (posts, questions, api2) returns 403 "only available to authenticated users" with plain and browser user agents. Throttle measured in front of the auth check: 9 calls in 1.9 s drew a 429 with Retry-After 10. Reruns unchanged once a token exists. Artefact: experiments/2026-09-24-P-0007
- Probe P-0008, **works** (1 min): Keyless path exists for the aggregate: DVSA anonymised MOT results on data.gov.uk, 2023 zip 1.19 GB, 3.66 GB CSV, Deflate64 so Python zipfile refuses it, lookup tables 254 KB. A named car needs the MOT History API (401 MOTH-UA-01 without a client key; free registration, human job) and the GOV.UK check page is a 403 bot wall to curl. Artefact: experiments/2026-09-24-P-0008
- Probe P-0009, **works** (0 min): Keyless reads work: 286 live Victoria line predictions in 0.27 s and every tube line status in 0.08 s. Anonymous limit measured at 48 calls a minute: the 49th in 12 s drew a 429 with Retry-After 48, no rate headers before it. Artefact: experiments/2026-09-24-P-0009
- Probe loop stopped 09:38: nothing runnable: 3 need something I do not have (P-0011: Lichess bot account (Luke's one-click); P-0014: a day-long poller (launchd job or hourly task), not a single-night probe; P-0015: MOT History API client id and key (free DVSA registration, a human signs up), in 1Password tagged proteus); 3 wait on a date (P-0012 after 2026-10-22; P-0013 after 2026-10-20; P-0016 after 2026-09-27); 3 do not fit the 21 min left (P-0004 30 min, P-0006 25 min, P-0010 25 min). 4 probe(s) (works 3, blocked 1), 5 min, calls not measured.
- Queued from the verdicts, needs declared: a day-long GeckoTerminal poller (P-0014), the MOT History API key (P-0015), and a 3.66 GB MOT bulk run dated for a quiet evening (P-0016). Asks for Luke, both already on the persona's one-click list: Metaculus token, MOT History API registration.
- Denied: none (interactive). Learned: GeckoTerminal lists pump.fun bonding-curve tokens as dex pump-fun, so graduation has a keyless numerator and denominator; Metaculus closed its read API to anonymous callers; DVSA bulk zips are Deflate64, which Python zipfile refuses; TfL anonymous limit is 48 a minute with Retry-After 48.

## Nightly run 2026-09-24 23:23
- Probe loop started 23:28: deadline 00:28 (nightly preflight 23:23 plus 80 min, capped at the loop's own 60 min), 150 calls, 6 probes, session 185ebb06.
- Probe P-0004, **not-worth-it** (1 min, 10 calls, 0 denied): On the desk's DC model the feature earns nothing: adjusted DC takes 0.00 pool weight against the close in every season across nine leagues (n=6766) and out-of-sample log loss moves 0.0006. Faint lead on the market only: pre-close shaded away from outlying Max prices takes pool weight 0.08 to 0.29 v close, a 0.0005 log-loss gain against a 0.0021 gap. Artefact: experiments/2026-09-24-P-0004
- Probe P-0006, **works** (0 min, 6 calls, 0 denied): Of 183 mints the Grinder first saw under a day old on 22 and 23 Sep, 83 (45 percent) still show any 24h volume, 23 (13 percent) at least 1k USD, 15 (8 percent) at least 10k, 30 traded in the last hour; the one-day cohort (n=29) reads 48/21/10/10. Only 4 of 108 pump.fun-curve launches moved to another dex. Unlisted read as dead, unproven. Artefact: experiments/2026-09-24-P-0006
- Probe P-0010, **works** (1 min, 7 calls, 0 denied): Spikes follow results, they do not precede them: pre-match abnormal views v points over the closing price r = -0.015 (CI -0.10 to +0.07, 569 team-matches), while the day after a win views run x1.38 and after a shock win about x1.95. Only 14 of 20 clubs: Wikimedia 429s the 15th call in two runs, with or without a 1s pause. Artefact: experiments/2026-09-24-P-0010
- Probe loop stopped 23:31: nothing runnable: 3 need something I do not have (P-0011: Lichess bot account (Luke's one-click); P-0014: a day-long poller (launchd job or hourly task), not a single-night probe; P-0015: MOT History API client id and key (free DVSA registration, a human signs up), in 1Password tagged proteus); 3 wait on a date (P-0012 after 2026-10-22; P-0013 after 2026-10-20; P-0016 after 2026-09-27). 3 probe(s) (works 2, not-worth-it 1), 3 min, 26 calls, 0 denied.
- Preflight CLEAR 23:23; halt-check before commit CLEAR.
- Grinder: 120 candidates, 117 snapshot rows. 2 exits (G-0001 FUNKOS stop loss -£6.63, G-0002 JEANCOIN take profit +£3.45), 2 entries (G-0005 Agrippa, G-0006 dap, £5 each), 4 open. No rugs flagged. Scored 0 (nothing at 24h yet).
- Pitch: data refreshed, eight league models refit to 20 Sep, 0 new predictions (0 fixtures in the 8-day window; next kickoff on file 9 Oct, the known international break in BACKLOG). Scored 0 of 0.
- Committed 6d0e12a "nightly: pre-register 2026-09-24" and pushed; probe commits c126ac5, 2afb034, ab6d418, 6708976.
- Field Notes (Thursday, wildcard): National Grid Carbon Intensity API, keyless, 48h forecast v actual: MAE 11.7 g (10.3%), bias -2.4. Written to drafts/2026-W39.md; SEEN.md appended and mirrored to the vault. Draft, SEEN and sandbox/carbon_forecast.py not yet committed; tomorrow's step 4 sweeps them.
- Queued P-0017 (desk): fill the six clubs P-0010 lost to Wikimedia 429s and measure the quota window.
- Denied: none. 57 hook-logged calls this session, all allowed. No sub-agents.
- Learned: Wikimedia pageviews stops at about 14 calls from here whatever the spacing; club attention follows results and does not lead them; bookmaker disagreement adds nothing to DC.
