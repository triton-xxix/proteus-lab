- Harvest: 72 candidates pulled, 12 shortlisted, 10 kept, 2 skipped, 1 queued as probes (P-0019 from H-0010); 0 dropped as already seen. Brief `state/harvest/2026-09-26/brief.md`, judgements `state/agents/2026-09-26/harvest.json`.

## Interactive session 2026-09-26 (the harvester, Luke's brief of the same day)

- Built `bin/harvest.py` (pull, shortlist, ingest, render, digest, review), queries in `field-notes/harvest-queries.json`, `harvest` added to probe.py's sources, HARVEST.md added to the vault mirror. Kill rule written into PASS-MARKS.md and committed (6db1ad8) before any entry existed.
- First harvest run under the unattended hook (marker bound to this session 11:13, released 11:22): 72 candidates (arxiv 22, github 28, hn 10, youtube 12; three awesome-list baselines saved, nothing taken), 12 shortlisted, 3 flagged with a vault verdict on the vendor, 0 dropped as seen.
- One Sonnet child (a7020cd9, general-purpose) judged the brief: 7 calls allowed, 3 denied (a `;` with `2>&1`; SubagentHandback twice). 12.2k output, 2.07M cache reads, 221k cache writes, 5 min 56 s. Agent tool reported 104,812 tokens, 10 tool uses.
- Ingest: 10 kept, 2 skipped, 1 queued (P-0019 from H-0010, hyperframes-student-kit). Committed and pushed fdf76d9. Parent: 13 calls allowed, 1 denied (a `;` in a grep, mine).
- Learned: the hook denied the child's SubagentHandback, so a child's report never reaches the parent in a hooked run. Added to FREE_TOOLS with a dated comment; the hook test harness was not run this session (classifier refused the read next to the hook edit), so that is for Luke to run.
- Learned: the child marked 1 of 12 testable, conservative; prompt now allows a narrower read-only question. HN matching is loose; YouTube queries are the weakest list.
- Scheduled: harvest is step 5b of the nightly, before the probe loop; weekly reads `harvest.py digest` for Watched and read. Live task prompts reloaded from the repo copies.
- Denied: 1 parent (hook, `;`), 3 child (hook), 1 classifier (compound command touching bin/test-hook.py after the hook edit).
- Sub-agents: 1 (a7020cd9, harvest judgement, sonnet, 7 allowed, 3 denied, ~105k tokens).
